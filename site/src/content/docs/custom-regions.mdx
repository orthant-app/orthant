---
title: Custom regions
description: Draw any block on any grid, name it, and give it a shortcut.
group: place
order: 2
---

import RegionDiagram from '../../components/RegionDiagram.astro';

The built-in commands cover halves, quarters, maximize and center. A custom region is
anything else: left two thirds, a centred column, the top strip of an ultrawide.

## Creating one

Two ways in:

- **From the overlay.** Make a selection and press `⌘S` *while the grid is still up*: either
  holding the mouse button down rather than releasing it, or after selecting with the arrow
  keys. Releasing a drag places the window and dismisses the overlay, which leaves nothing to
  press `⌘S` on. The picker opens already filled in, with a suggested name.
- **From Settings.** Open **Settings ▸ Shortcuts** and choose *Add region…*, then draw on the
  grid in the sheet. Arrow keys and `⇧`-arrows work here too.

Then name it, click the shortcut field, press the combination you want, and click **Add**.

## A region remembers its own proportions

<div class="compare-grids">
  <figure>
    <RegionDiagram cols={3} rows={1} c0={0} c1={1} r0={0} r1={0}
      title="Left two thirds on a three-column grid" label="2/3" />
    <figcaption>Drawn on a 3-column grid</figcaption>
  </figure>
  <figure>
    <RegionDiagram cols={12} rows={1} c0={0} c1={7} r0={0} r1={0}
      title="The same region on a twelve-column grid" label="8/12" />
    <figcaption>The same region, 12-column grid</figcaption>
  </figure>
</div>

So you can set the grid to 6 columns to draw a precise third, set it back to 4, and the region
still gives you a third.

## Editing and deleting

Select a region in **Settings ▸ Shortcuts** to rename it, redraw it, or change its shortcut.
Editing keeps the same region, so its shortcut survives.

Deleting a region also clears its shortcut, and offers an Undo.

*Reset Shortcuts* puts the built-in commands back to their defaults. Your regions survive it;
their shortcuts do not.
